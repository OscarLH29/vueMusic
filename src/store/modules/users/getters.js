const userList = (state) => state.userList;
const loginR = (state) => state.login;


//---------------------------------------------------------------
const isLoading = (state) => state.isLoading;
const user = (state) => state.user;

const deleteU = (state) => state.deleteU;

//---------------------------------------------------------------

export default {
    userList,
    loginR,


    //------------
    isLoading,
    user,

    deleteU
}
